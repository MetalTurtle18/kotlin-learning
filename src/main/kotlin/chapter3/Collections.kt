package chapter3

fun main() {
    val map = hashMapOf(1 to "one", 2 to "two", 3 to "three", 4 to "four") // This is the same HashMap as Java's
    val numbers = setOf(5, 3, 18, -8)
    println(numbers.maxOrNull()) // You can do more in Kotlin than with Java collections
}